# Base pack — shared `lib/` patterns

This document describes cross-cutting patterns in `lib/**` (auth helpers, DB access, email, ticket utilities) so agents and humans keep behavior consistent.

When you introduce or materially change shared library code, update this file in the same PR.

## Topics

_Add sections for major areas (e.g. sessions, permissions, ticket lifecycle) as they stabilize._
