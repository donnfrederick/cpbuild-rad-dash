/** Dispatched after a new ticket is created (global inbox refresh). */
export const TICKETS_INBOX_REFRESH_EVENT = "tickets-inbox-refresh";

/** Dispatched with `CustomEvent.detail` = TicketReport for in-memory list row merge. */
export const TICKETS_INBOX_MERGE_EVENT = "tickets-inbox-merge";
